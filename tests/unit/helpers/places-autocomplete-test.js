import { placesAutocomplete } from 'ubikate-umss-web/helpers/places-autocomplete';
import { module, test } from 'qunit';

module('Unit | Helper | places autocomplete');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = placesAutocomplete([42]);
  assert.ok(result);
});
